made sim model with following idea for classification
take train data and bucket based on label
    use top k elements
    for any new data point
     compute its distance from top k
     aggregate i.e. max,min average
     assign to bucket with smallest distance

Everything runs now without errors but is total nonsense
Issues:
    usually you make an image a giant vector here it needs to be 2 dimensional so essentially we take the characteristic
    function on the points.
    I need to make sure we can shift and rescale the points after that I think me and curtis made code for this
    I need to figure out the diameter thing obviously the shift and rescale would take care of that
    Looking at the dataset it seems possible that even if the method works well it will do poorly on this task since
    a lot of the digits are generated by non isometric moves, i.e fatter or skinnier 3's 7's etc

1/20
We have decided you cannot rescale images since the distance will not be preserved were just representing them as a
characteristic function on the points

The pipeline is set up to suck in a data set and then predict on samples
at the moment it is very slow though ~ 1 day to run inference on the full test set for mnist with bad parameters,
even worse the images are very small here, would get even slower on anything you actually care about

Checked and Timed different distance calculations, voronoi is slow and not returning correct distances yet vlad working
on fix now.

The images all live fairly close to each other so by paying the price of computing the diameter each time we can trade
that for drastically reducing the size of the shifts. My hope then is that if the shifts are smaller we can get away
with sampling much fewer points in the interval for them. This or a similar idea speeds things up massively since the
shifts are the biggest part of the grid search  = |xshift||yshift||theta|*2

